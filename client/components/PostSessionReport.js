import React from 'react';
import { connect } from 'react-redux';

const PostSessionReport = () => {
  return (
    <div>
      <div>SESSION SUCCESSFUL/FAILED</div>
    </div>
  );
};

export default connect(null)(PostSessionReport);
